<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IbzzOfficial</title>
    <link href="https://unicons.iconscout.com/release/v4.0.0/css/line.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/sweetalert2@11.7.2/dist/sweetalert2.min.css" rel="stylesheet">
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #1c1c1c;
            color: #fff;
        }
        .header {
            background-color: #1c1c1c;
            padding: 10px 20px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            position: relative;
        }
        .logo {
            font-size: 24px;
            font-weight: bold;
            color: #ff4500;
        }
        .nav-links {
            display: flex;
            gap: 20px;
        }
        .nav-links a {
            color: #fff;
            text-decoration: none;
            font-size: 16px;
            font-weight: bold;
            position: relative;
            padding-bottom: 5px;
        }
        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            height: 2px;
            background-color: #ff4500;
            width: 0;
            transition: width 0.3s ease;
        }
        .nav-links a.active::after {
            width: 100%;
        }
        .nav-links a:hover {
            color: #ff4500;
        }
        .search-input {
            background-color: #e4e5f1;
            padding: 5px;
            border-radius: 5px;
            border: none;
            color: #2c2c2c;
            position: absolute;
            top: 100%;
            left: 0;
            right: 0;
            margin: 0 10px;
            display: none;
            outline: none;
        }
        .menu-icon, .search-icon {
            font-size: 24px;
            color: #fff;
            cursor: pointer;
            display: none;
            font-weight: bold;
        }
        .nav-menu {
            position: fixed;
            top: 0;
            left: -100%;
            width: 250px;
            height: 100%;
            background-color: #2c2c2c;
            padding: 20px;
            transition: left 0.3s ease;
            z-index: 99;
        }
        .close-icon {
          font-size: 30px;
        }
        .nav-menu.active {
            left: 0;
        }
        .nav-menu .close-icon {
            position: absolute;
            top: 10px;
            right: 10px;
            cursor: pointer;
            font-weight: bold;
        }
        .nav-menu .nav-logo {
            font-size: 24px;
            font-weight: bold;
            color: #ff4500;
            margin-bottom: 20px;
        }
        .nav-a {
          text-decoration: none;
          color: #ff4500;
        }
        .nav-link {
            display: flex;
            align-items: center;
            padding: 10px 0;
            color: #fff;
            text-decoration: none;
            font-weight: bold;
        }
        .nav-link i {
            margin-right: 10px;
            font-weight: bold;
        }

        @media (max-width: 768px) {
            .nav-links {
                display: none;
            }
            .menu-icon, .search-icon {
                display: block;
            }
            .search-input {
                display: none;
                margin-top: 10px;
                width: calc(100% - 40px);
            }
            .search-icon.active + .search-input {
                display: block;
            }
        }

        @media (min-width: 769px) {
            .menu-icon, .search-icon {
                display: none;
            }
            .search-input {
                display: block;
            }
        }
    </style>
</head>
<body>
    <div class="header">
        <div class="menu-icon" onclick="toggleMenu()">
          <svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 24 24"><path fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25H12"/></svg>
        </div>
        <div class="logo">
          <a class="nav-a" href="/">IbzzOfficial</a>
        </div>
        <div class="nav-links">
            <a href="#" class="nav-link" onclick="selectNavLink(this)"><i class="uil uil-estate"></i> Home</a>
            <a href="#" class="nav-link" onclick="selectNavLink(this)"><i class="uil uil-box"></i> Products</a>
            <a href="#" class="nav-link" onclick="selectNavLink(this)"><i class="uil uil-info-circle"></i> About</a>
            <a href="#" class="nav-link" onclick="selectNavLink(this)"><i class="uil uil-envelope"></i> Contact</a>
        </div>
        <div class="search-icon" onclick="toggleSearch()"><i class="uil uil-search"></i></div>
        <input type="text" class="search-input" placeholder="Search...">
    </div>
    <div class="nav-menu" id="navMenu">
        <div class="close-icon" onclick="toggleMenu()"><i class="uil uil-times"></i></div>
        <div class="nav-logo">
          <a class="nav-a" href="/">IbzzOfficial</a>
        </div>
        <a href="#" class="nav-link"><i class="uil uil-estate"></i> Home</a>
        <a href="#" class="nav-link"><i class="uil uil-box"></i> Products</a>
        <a href="#" class="nav-link"><i class="uil uil-info-circle"></i> About</a>
        <a href="#" class="nav-link"><i class="uil uil-envelope"></i> Contact</a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11.7.2/dist/sweetalert2.all.min.js"></script>
    <script>
        function toggleMenu() {
            document.getElementById('navMenu').classList.toggle('active');
        }

        function toggleSearch() {
            document.querySelector('.search-icon').classList.toggle('active');
        }

        function selectNavLink(link) {
            const links = document.querySelectorAll('.nav-links a');
            links.forEach(l => l.classList.remove('active'));
            link.classList.add('active');
        }
    </script>
</body>
</html>
